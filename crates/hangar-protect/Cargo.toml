[package]
name = "hangar-protect"
edition.workspace = true
license.workspace = true
version.workspace = true

[features]
default = ["core"]
core = []
mutation = []
agent_automation = []

[dependencies]
hangar-core = { path = "../hangar-core" }

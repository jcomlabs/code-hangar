# Fixture Sensitive Project

This fixture validates that sensitive files are visible as blocked entries without revealing their contents.

Open `.env`, `credentials.json` or `token.json` to verify the preview-blocked state.
